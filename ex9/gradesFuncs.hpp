#include <fstream>

using namespace std;

//main function that takes an input file, calculate the result and puts it into the output file
void produceResult(ifstream& in, ofstream& out);

//function to calculate the total of the grades
void totalOfGrades(ifstream& in, ofstream& out);
